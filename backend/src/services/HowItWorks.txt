User fills form
      ↓
data object sent to backend
      ↓
analyzeBond(data) in finalScorer.js
      ↓
┌─────────────────────────────────┐
│ getIssuerScore(data)            │ → how strong is the company?       --> scoringEngine.js
│ structureScore (inline lookup)  │ → how safe is the bond structure?  --> finalScore.js
│ liquidityScore (inline lookup)  │ → how easy to exit?                --> 
│ getExpectedLoss(data)           │ → PD × LGD → risk adjusted return  --> getExpectedLoss.js  
│ returnScore (inline lookup)     │ → is the yield worth it?
│ getFitScore(data, issuerScore)  │ → does this bond suit this user?   --> getFitScore.js
└─────────────────────────────────┘
      ↓
finalScore = weighted average of all 5
      ↓
ratingBand from finalScore
      ↓
return everything to frontend